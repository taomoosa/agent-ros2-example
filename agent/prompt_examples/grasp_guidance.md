For this application, handle blue rectangular blocks and place them on the tray.
Prefer a visible, textured grasp surface near the object's center, away from
thin protrusions and neighboring objects. For a two-arm shared object, choose
opposite ends consistently. The release point should be on the visible tray
support surface. If a valid contact point is occluded, return no targets.
Keep the required JSON keys and normalized [y, x] coordinate convention.
